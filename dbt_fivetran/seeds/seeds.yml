version: 2

seeds:
  - name: matched_job_titles
    config:
      schema: salary_guide

  - name: reference_job_titles
    config:
      schema: salary_guide

  - name: eh_pay_category_mapping
    config:
      schema: pay_category_mappings

  - name: kp_2022_2023_pay_category_mapping_part1
    config:
      schema: pay_category_mappings

  - name: kp_2022_2023_pay_category_mapping_part2
    config:
      schema: pay_category_mappings

  - name: organisations_test
    config:
      schema: exports

  - name: accounts_test
    config:
      schema: exports

  - name: calling_codes
    config:
      schema: workshop_public
