version: 2

sources:
  - name: marketing
    tables:
      - name: users_from_snapshot
        columns:
          - name: email
            meta:
              sensitive: true
              masking_rules:
                full_mask:
                  mask_function: sha2-256
      - name: users_snapshot
        columns:
          - name: email
            meta:
              sensitive: true
              masking_rules:
                full_mask:
                  mask_function: sha2-256