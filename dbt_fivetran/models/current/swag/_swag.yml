version: 2

sources:
  - name: swag
    tables:
      - name: jobs_events
        columns:
          - name: user_email
            meta:
              sensitive: true
              masking_rules:
                full_mask:
                  mask_function: sha2-256
