version: 2

models:
  - name: stg_eh_infra_stat_service_raw__daily_report_sentry_issues
    description: "This is the staging table for daily_report_sentry_issues, sourced directly from eh_infra_stat_service_raw.daily_report_sentry_issues table. This table excludes the rows deleted by fivetran"
    schema: staging
    materialized: view
    meta: 
      upstream_system_owner: "squad-bahamut"
      data_owner: "gold-diggers"
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: issues_id
        tests:
          - not_null