version: 2
models:
  - name: stg_heropay_db__heropay_transactions
    description: This is the staging table for heropay_transactions, sourced directly from heropay_db_public.heropay_transactions table
    meta: 
      upstream_system_owner: "squad-squid"
      data_owner: "gold-diggers"
    columns:
      - name: id
        tests:
          - unique
      - name: heropay_balance_id
        tests:
          - not_null
      - name: employee_uuid
        tests:
          - not_null
      - name: transaction_status_key
        tests:
          - not_null
      - name: transaction_amount
        tests:
          - not_null
